# 隐私说明

本说明适用于 Codex 文档产物 `v0.1.1`。本项目的目标是让文档索引留在你的电脑上，而不是建立一个云端文档库。

## 一句话结论

默认情况下，程序只在本机读取 Codex 留存的最终交付记录、建立本地索引，并通过 `127.0.0.1` 提供页面；它不会上传文档，也不会发送遥测数据。长期记忆和 Office 快照默认关闭。

## 默认会读取什么

为了发现并展示文档，程序会在当前操作系统用户权限范围内读取：

- Codex 的本机任务索引和项目状态，用于找到仍然保留的顶层任务及其项目名称。
- 这些任务对应的本机历史记录，主要检查用户请求上下文和助手最终回复。
- 最终回复中出现的受支持在线链接，以及 Office、Markdown 文件路径。
- 受支持本地文件的存在状态、大小和修改时间等基础信息。
- Markdown 文件内容，用于本地阅读、提取标题，以及执行你配置的自定义归类规则。单个文件有大小限制。
- 你主动上传的自定义文档库 Logo，以及你填写的归类规则。

扫描范围默认为**本机仍保留的全部 Codex 历史至今**。程序不会为了补齐缺失历史而从云端恢复已经不在本机的数据。

## 默认不会读取什么

- 不会遍历整块硬盘寻找所有文档。
- 不会读取与 Codex 最终交付无关的浏览器历史、邮件、聊天记录或其他应用数据。
- 不会自动读取长期记忆和定期任务配置；此能力默认关闭。
- 不会为了建立索引而读取 Office 文档正文。Office 快照关闭时，只记录路径和基础文件信息。
- 不会绕过在线文档平台的账号或访问权限。

## 会在本机保存什么

默认数据目录是 `~/.codex/document-artifacts`。其中可能包括：

- 文档标题、类型、在线地址或本地路径。
- 项目名、任务名、任务标识、交付时间和一小段相关请求上下文。
- 同一文档的交付记录、扫描进度和文件可用状态。
- 自定义文档库的名称、顺序、Logo 和匹配规则。
- 已解析的平台标题缓存。
- 可选 Office 快照产生的本地副本。

这些数据不会由项目自动加密；数据目录强制设为仅当前用户可访问，数据库和快照也只允许当前用户读写。它们还受到你的操作系统账号和磁盘加密保护。共用电脑上的其他管理员，或已经控制你账号的恶意程序，仍可能读取这些信息。

数据会一直保留，直到你手动删除。删除 `~/.codex/document-artifacts` 会清除本模块的索引和缓存，但不会删除原始 Codex 历史或原文档。

## 网络行为

### 默认本地运行

- 服务默认只绑定 `127.0.0.1:47824`。
- 服务会校验请求来源、Host 和 Origin 必须是本机回环地址。
- 项目没有账号系统、云同步、分析统计或遥测上报。
- 安装依赖、下载版本或检查项目更新时，包管理器和 GitHub 会产生正常网络请求，但文档内容不会因此发送给它们。

### 你主动打开在线文档时

点击“打开”会把链接交给系统浏览器。随后产生的网络请求由浏览器和原文档平台处理，并受它们各自的隐私政策约束。本模块不会代理该页面。

### 你主动复制链接时

“复制链接🔗”只把当前网址或本地路径写入系统剪贴板，不会把内容发送到网络。剪贴板可能被你随后粘贴到的应用读取；分享前请检查是否包含敏感路径或访问参数。

## 两项默认关闭的可选能力

| 可选能力 | 默认 | 启用后新增读取或存储 | 是否可能外发 |
| --- | --- | --- | --- |
| 长期记忆 | 关闭 | 读取 Codex 长期记忆、定期任务配置及其中明确引用的少量文本文件，用于生成“长期文档”视图 | 默认实现不外发 |
| Office 快照 | 关闭 | 读取受支持 Office 文件并在本机数据目录保存只读副本；可能显著增加磁盘占用 | 不外发 |

若你自行接入第三方标题解析器或其他扩展，该扩展可能访问在线链接。此类自定义扩展不属于默认网络行为，应单独审查它的代码和隐私政策。

## 你可以怎样减少数据暴露

- 使用默认配置，不启用暂时用不到的可选能力。
- 不要把本地服务暴露到局域网或公网。
- 启用操作系统磁盘加密，并锁好本机账号。
- 分享截图、日志或 Issue 前，遮盖文档标题、链接、文件路径、项目名和任务内容。
- 定期删除不再需要的本地索引或快照。
- 复制本地文件路径后，粘贴前先确认接收方和内容。

## 如何删除本模块数据

先停止服务，然后删除 `~/.codex/document-artifacts`。该操作会移除索引、缓存、自定义分类和快照；不会删除原始文档，也不会修改 Codex 自己的任务历史。

只删除程序代码目录，不会自动删除上述本地数据目录。

## 隐私问题和安全问题

一般隐私问题可以在 [GitHub Issues](https://github.com/RuiChenKe/codex-document-artifacts/issues) 提问，但请勿附带真实文档、私密链接、访问令牌或完整本地路径。

如果问题可能导致未授权读取或外发数据，请不要公开细节，改按 [SECURITY.md](SECURITY.md) 使用 GitHub Security Advisories 私下报告。

## 本说明的更新

如果未来新增默认联网、遥测、云同步或扩大本机读取范围，应在代码合并前同步更新本说明，并在 Release Notes 中明确告知用户。
