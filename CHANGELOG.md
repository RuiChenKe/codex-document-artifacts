# 更新记录

## v0.1.1

- 把 Codex 侧边栏入口提升为 README 的首要介绍，并替换为包含真实侧边栏的脱敏演示图。
- 文档卡片铺满主内容区，不再在宽屏下留下大块空白。
- 恢复飞书、企业微信、Word、Excel 和 PowerPoint 的文档类型 Logo。
- 修复服务重启时旧连接偶发得到 `Internal server error` 的关闭竞态。
- 只读接口会自动重试重启期间的短暂网络错误与 5xx；文档库配置失败也不会清空已经加载的文档列表。
- 默认扫描本机仍保留的全部历史，初始时间筛选为“全部时间”。
- 扫描器升级完成回扫后才登记新版状态，避免中途状态不一致。

## v0.1.0

- 首个公开预览版。
