# 安全政策

Codex 文档产物会处理本机文档路径、链接和 Codex 任务上下文。请把可能导致这些信息被未授权读取、修改或发送的问题视为安全问题，并私下报告。

## 受支持版本

当前仍处于 `0.x` 公开预览阶段，只为最新发布版本提供安全修复：

| 版本 | 安全修复 |
| --- | --- |
| `0.1.x` 最新 Release | 支持 |
| 更早版本 | 不保证 |

请从本仓库的 [Releases](https://github.com/RuiChenKe/codex-document-artifacts/releases) 获取正式版本，并及时阅读升级说明。

## 私下报告漏洞

请优先进入仓库的 [Security 页面](https://github.com/RuiChenKe/codex-document-artifacts/security)，使用 **Report a vulnerability** / **Security Advisories** 私下提交报告。不要把可利用细节、真实文档、私密链接、令牌或本机路径发布到普通 Issue。

报告中最好包括：

- 受影响版本、操作系统和 Node.js 版本。
- 问题的影响，以及攻击者需要具备什么条件。
- 最小化、可重复的步骤或概念验证。
- 已去除隐私信息的日志或截图。
- 如果已经有想法，可附上修复建议。

如果 Security 页面没有显示私下报告入口，请只创建一条不含漏洞细节的普通 Issue，请求维护者开启私密沟通渠道。不要先公开完整漏洞。项目目前不公布也不虚构安全邮箱。

维护者会尽力确认报告、评估影响并协调修复与披露，但当前没有承诺固定响应时限。请在修复版本发布前保密。

## 安全边界

默认配置包含以下保护：

- HTTP 服务只监听本机回环地址 `127.0.0.1`。
- 请求的远端地址、Host 和 Origin 必须是回环地址。
- 页面带有内容安全策略、同源限制、禁止嗅探等安全响应头。
- 长期记忆读取和 Office 快照默认关闭。
- 默认没有云同步、遥测或文档上传服务。

macOS 侧边栏嵌入模式会在运行期间通过只绑定 `127.0.0.1` 的 Chromium 调试接口连接 Codex 桌面版，以注入“文档产物”入口。为了让 Codex 主页面加载本机文档 iframe，启动器会在运行期间对该 renderer 暂时绕过内容安全策略（CSP），停止时恢复；这会削弱该窗口运行期间的一层浏览器防护。只应运行从正式 Release 获取并核验过的代码，也不要让不受信任的本地程序访问调试端口。不需要嵌入时可改用 `npm start` 网页模式，它不会连接或修改 Codex renderer。

但本项目不是多用户服务器，也没有登录、权限分级或公网部署能力。它信任当前操作系统用户能够读取自己的 Codex 数据和文档。已经控制本机账号的攻击者、恶意本地程序，以及操作系统管理员，不在它能够完全防御的范围内。

## 安全使用建议

- 不要把监听地址改成 `0.0.0.0`，不要通过反向代理、端口转发、内网穿透或公网隧道公开服务。
- 不要以管理员或 `root` 身份运行。
- 使用 macOS 嵌入模式时，不要把本机调试端口转发到其他设备，也不要在不受信任的共用账号中运行。
- 只安装本仓库正式 Release；保持 Node.js、浏览器、Codex 和本项目版本更新。
- 启用可选功能前阅读 [PRIVACY.md](PRIVACY.md)，确认新增的本机读取和存储范围可以接受。
- 共用电脑上建议启用独立系统账号和磁盘加密。
- 提交日志或截图前，删除文档链接、文件路径、任务内容、Cookie、令牌和其他凭据。
- 复制链接后谨慎粘贴；本地路径可能暴露用户名和目录结构，在线链接可能包含访问参数。

## 建议报告的安全问题

包括但不限于：

- 非本机设备能够访问服务或绕过回环地址检查。
- 恶意网页能够绕过 Origin / Host 检查调用本地接口。
- 路径穿越、任意文件读取、任意文件写入或命令执行。
- 未经用户启用就读取长期记忆或创建 Office 快照。
- 文档内容、路径、链接或任务信息意外发往外部网络。
- 跨站脚本、恶意 Markdown 渲染或自定义 Logo 导致的代码执行。
- 依赖供应链问题，或安装、更新流程可以被劫持。

普通功能缺陷、界面建议和不包含敏感细节的兼容性问题，可以使用 [GitHub Issues](https://github.com/RuiChenKe/codex-document-artifacts/issues)。如果不确定是否属于安全问题，请按安全问题私下报告。

## 协调披露

请给维护者合理时间完成复现、修复和发布。修复完成后，维护者可以通过 GitHub Security Advisory 和 Release Notes 公开影响版本、缓解措施与致谢；报告者可说明希望使用的署名，或选择匿名。
